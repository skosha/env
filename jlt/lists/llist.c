#include "common.h"
#include "llist.h"

void _llist_insert_head(struct _llist_list *list, struct _llist_node *node)
{

}

void _llist_insert(llist_list(list), llist_node(_n))

void _llist_insert_after(llist_list(list), llist_node(_n), llist_node(_a))
